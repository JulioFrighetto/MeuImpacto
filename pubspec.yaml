name: meu_impacto_app
description: Calculadora de Pegada Ecológica
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.2.2
  share_plus: ^7.2.1
  intl: ^0.18.1

flutter:
  uses-material-design: true  # ← ESTA LINHA É ESSENCIAL!